server "_box_post" {
    alias "post.b0x.pw"
    alias "p.b0x.pw"
    alias "my.post.b0x.pw"
    alias "mail.b0x.pw"
    alias "my.mail.b0x.pw"

    listen on $loopback port $httpd_plain_port
    listen on $loopback port $httpd_tls_port

    errdocs "/htdocs/box_main/err"

    log {
        style forwarded
        access "access.box_post.log"
        error "error.box_post.log"
    }

    location * {
        root "/htdocs/box_post"
    }
}